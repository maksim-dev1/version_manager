### Данные тепловой карты — активность по часам и датам.
class: HeatmapEntry
fields:
  ### Дата (YYYY-MM-DD, UTC)
  date: DateTime
  ### Час дня (0-23)
  hour: int
  ### Количество проверок
  count: int
